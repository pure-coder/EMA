import React, {Component} from 'react'
import {withRouter} from 'react-router-dom';
import {connect} from 'react-redux';



class NewClientProgress extends Component {
    constructor(props){
        super(props)
        this.state = {

        }
    }


    render(

    ); // render
}
export default connect()(withRouter(NewClientProgress));
